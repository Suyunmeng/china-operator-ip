name: Retired IP2Proxy Workflow

# IP2Proxy/IP2Location is intentionally not used. This file remains as a tombstone
# so existing workflow links explain why the old scheduled job disappeared.
on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "IP2Proxy was removed from the asset ownership pipeline."
          echo "BGP visibility and authoritative RIR WHOIS now drive generation."
