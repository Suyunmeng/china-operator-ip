[package]
name = "networksdb-filter"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
bgpkit-parser = { version = "0.12", default-features = false, features = ["local", "lz"] }
clap = { version = "4.5", features = ["derive"] }
ipnet = "2.11"
